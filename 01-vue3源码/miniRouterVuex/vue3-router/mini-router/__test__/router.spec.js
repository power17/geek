// const { JSDOM } = require("jsdom");
// const { window } = new JSDOM(`...`);
describe("test router", () => {
  const { createRouter } = require("./../router");
  it("test createRouter", () => {
    // createRouter();
    // console.log(11);
    // expect(createRouter()).toBe(true);
  });
});
